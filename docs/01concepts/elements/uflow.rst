Uniform Flow
============

Uniform flow represents regional groundwater flow with a constant gradient.

1. :class:`~timml.uflow.Uflow` adds uniform flow to a confined aquifer model. It is
   specified by a slope (head gradient) and angle (direction of flow).
